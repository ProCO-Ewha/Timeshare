import Topbar from "../components/Topbar.js";

function Store() {
  return (
    <div
      style={{
        margin: "40px 150px",
      }}
    >
      <h1>store</h1>
      <Topbar now="store" />
    </div>
  );
}

export default Store;
